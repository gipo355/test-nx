# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build test-nx`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t test-nx`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system test-nx && \
          adduser --system -G test-nx test-nx

COPY dist/apps/test-nx test-nx
RUN chown -R test-nx:test-nx .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix test-nx --omit=dev -f install

CMD [ "node", "test-nx" ]
