//- TODO: mixin here for path constant
header.header
  nav.nav.nav--tours
    div
      a.nav__el(href='/') All tours
      // form.nav__search
      //   button.nav__search-btn
      //     svg
      //       use(xlink:href='img/icons.svg#icon-search')
      //   input.nav__search-input(type='text' placeholder='Search tours')
      a.nav__el(href='/docs') API
      // a.nav__el(href='/typedoc') Types
  .header__logo
    img(src='/public/img/logo-white.png', alt='Natours logo')
  nav.nav.nav--user
    if user
      //- - console.log(user);
      //- a.nav__el(href='/me') My account
      //- a.nav__el(href='#') My bookings
      //- a.nav__el.nav__el--logout(href='/logout') Logout
      a.nav__el.nav__el--logout Logout
      - const path = user.photo.startsWith('http') ? `${user.photo}` : `/public/img/${user.photo}`;
      a.nav__el(href='/me')
        img.nav__user-img(
          src=path,
          crossorigin='anonymous',
          alt=`Photo of ${user.name}`
        )
        span= user.name.split(' ')[0]
    else
      a.nav__el(href='/login') Login
      a.nav__el.nav__el--cta(href='/signup') Sign up
    //- <button class="nav__el">Log in</button>
    //- <button class="nav__el nav__el--cta">Sign up</button>
